const express = require("express");
const router = express.Router();
const logincontroller = require("../controllers/logincontroller");

// har controller ke function ka app.method

module.exports = router;
