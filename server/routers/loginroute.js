const express = require("express");
const router = express.Router();
const logincontroller = require("../controllers/logincontroller");

// app.METHOD('/', logincontroller.function)

module.exports = router;
